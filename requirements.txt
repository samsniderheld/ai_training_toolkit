# Core dependencies
pillow>=10.0.0
tqdm>=4.65.0
pyyaml>=6.0

# OpenAI for captioning
openai>=1.0.0

# Google Gemini for captioning (optional)
google-genai>=0.2.0

# GCP dependencies for batch training
google-cloud-batch>=0.17.0
google-cloud-storage>=2.10.0
google-auth>=2.23.0
google-cloud-logging>=3.5.0

# AI Toolkit dependencies (add if needed)
# torch>=2.0.0
# diffusers>=0.21.0
